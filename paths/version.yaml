get:
  summary: Version of the ontology API
  description: Get ontology API version metadata
  operationId: getVersion
  tags:
    - Version
  responses:
    200:
      description: Successfully returned the APIs version.
      content:
        application/ld+json:
          schema:
            $ref: '../schemas/ApiMetadata.yaml'